# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
.next/
mcp-server/dist/

# Database
*.db
*.db-journal

# IDE
.idea/
.vscode/
.claude/*
!.claude/mcp.json
.cursor/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

/lib/generated/prisma

# Python virtual environment
.venv/

# Backups
backups/

# Internal session/planning files
HANDOVER.md
plan.md
scripts/check-handover.sh

# Playground pages
app/dither/

# Private local docs (cloud prompts, notes — not for public repo)
.private/

# Store listing / submission copy — competitive surface, keep local
extension/store-listing*.md
extension/STORE-SUBMISSION.md

# Email templates + send scripts — business IP, lives in cloud repo (private fork)
# Old commit 9c1c385 leaked these to public OSS history; do not re-add here
emails/
scripts/render-emails.ts
scripts/send-email-test.ts

# Internal build prompts / roadmap docs — never publish to OSS
docs/ytt-*-build-prompt.md

# Tester checklist — internal until product is ready for public beta
docs/TEST_USER_CHECKLIST.md

# Private strategy / marketing / branding docs — never commit, even if copied in
# (these normally live in the parent transcriber/ folder which is not a git repo)
/0*-*.md
/1*-*.md
/competitive-analysis*.md
/competitor-*.md
/*-pitch-deck.*
/*-investor-*.md
/*-investment-*.md
/marketing/
/brand/
/design/
/_marketing-validation/
tmp/email-renders/

# Chrome unpacked dev keypair — never commit
extension/dev-key.pem
